---
epic: 4
date: 08-31-2026
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 4 Retrospective — Student Progress, Levels & Parent Meetings

## Epic summary

**Epic:** 4 — Student Progress, Levels & Parent Meetings  
**Diff range:** `366f12e^..HEAD` (19 commits, 0 merges)  
**Stories completed:** 7/7 — all `done`

| Story | Status |
|-------|--------|
| 4-1-auto-generated-student-dossier | done |
| 4-2-hero-curve-collapsed-dictation-table-c1 | done |
| 4-3-promotion-banner-on-dossier-d1 | done |
| 4-4-manual-level-override-level-history | done |
| 4-5-promotion-detection-rules | done |
| 4-6-alertes-promotion-queue-d2 | done |
| 4-7-presentation-mode-rdv-parents-c3 | done |

**Pending stories:** none (`detect-epic --epic 4`, 08-31-2026).

**Note:** The diff range includes two non–Epic 4 commits interleaved during delivery: archive-flow polish (`83f5e756`) and roster promotion UI polish (`55d26d49`, `cb87b088`). Epic 4 evidence is attributed by commit subject and file churn, not range contiguity.

### Evidence inventory

| Artifact | Status | Source |
|----------|--------|--------|
| Epic spec / context | available | `_bmad-output/implementation-artifacts/epic-4-context.md`, `_bmad-output/planning-artifacts/epics.md` lines 595–711 |
| Story specs (7) | available | `_bmad-output/implementation-artifacts/spec-4-*.md` (all `status: done`) |
| Sprint status | available | `_bmad-output/implementation-artifacts/sprint-status.yaml` |
| Git diff + commits | available | `git_evidence.py` on `366f12e^..HEAD` |
| Deferred work log | available | `_bmad-output/implementation-artifacts/deferred-work.md` |
| Architecture spine | available | `_bmad-output/planning-artifacts/architecture/` |
| Previous retrospective | available | `_bmad-output/implementation-artifacts/epic-3-retro-08-28-2026.md` |
| CI workflow | available | `.github/workflows/champions-app.yml` |
| Session logs | absent | no conversation/session records referenced in story artifacts |

`bmad-review` was not invoked as a separate sub-agent run; code lenses (adversarial, edge-case, verification-gap) were applied inline over the epic diff range, weighted at story boundaries (4-1→4-2 dossier composition, 4-3→4-5 promotion cross-surface, 4-6→4-7 presentation/chrome).

---

## Findings

### Architecture delta

**Layering respected with one type-only exception.** Presentation (`app/`, `components/`) → services (`lib/services/`) → domain (`lib/domain/`) → DB (`lib/db/`). No runtime imports from `services` or `db` inside domain except `import type` from `get-student-dictation-history` in `dossier-curve.ts` and `dossier-presentation.ts`.

- **Source:** `grep` on `champions-app/lib/domain/` (08-31-2026).
- **Disposition:** accept — type-only coupling to service DTO; consider moving `StudentDictationHistoryEntry` to domain if reused further.

**New dossier cluster introduced coherently.** `components/dossier/` (curve, table, presentation, level history), `lib/domain/dossier-curve.ts`, `lib/domain/dossier-presentation.ts`, `lib/services/get-student-dictation-history.ts`, `get-student-level-history.ts`.

- **Source:** `git_evidence.py`; `epic-4-context.md` lines 36–40.
- **Disposition:** accept as-is.

**Promotion logic refactored out of `dictation-save.ts`.** Story 4-5 extracted re-evaluation into `reevaluate-pending-promotion.ts` (+162 net); `dictation-save.ts` net −117 over the epic range. Detection rules remain in `lib/domain/promotion.ts` per NFR3.

- **Source:** `git_evidence.py` — `dictation-save.ts` net −117, `reevaluate-pending-promotion.ts` +162; commit `0ec2419`.
- **Disposition:** accept — improves separation; `reevaluate-pending-promotion.test.ts` (441 lines) covers FR31 streak reset.

**`students/actions.ts` became the dossier/roster/Alertes mutation hub** (369 lines, +164 net). Centralizes `overrideStudentLevelAction`, `validateDossierPromotionAction`, `refuseDossierPromotionAction`, and `revalidateDossierPromotionPaths` — consistent with Epic 2/3 tab-scoped action files.

- **Source:** `wc -l app/(dashboard)/students/actions.ts`; `git_evidence.py`.
- **Disposition:** accept — monitor size; split only if Epic 5 adds more student mutations.

**Presentation mode hides dashboard chrome client-side.** `dashboard-chrome.tsx` uses `pathname.endsWith("/present")` to strip AppBar/NavTabs on `/students/[id]/present`.

- **Source:** `components/dashboard/dashboard-chrome.tsx:20-24`; `deferred-work.md` lines 257–258.
- **Disposition:** defer — brief hydration flash possible; tighten path match if more `/present` routes appear.

### Duplication map

**Validate/refuse client orchestration duplicated across four surfaces.** Grid (`class-grid.tsx` → `dictations/actions.ts`), dossier (`promotion-banner.tsx` → `students/actions.ts`), roster (`roster-promotion-action.tsx`), and Alertes (`alerts-promotion-queue.tsx`) each wrap the same server actions with local pending/error state.

- **Source:** `deferred-work.md` lines 8–9 (story 4-6 review); `grep` validate/refuse across `champions-app/`.
- **Disposition:** defer — shared `usePromotionAction` hook or thin wrapper; pre-existing pattern amplified by Epic 4 surface count.

**Grid promotion actions do not revalidate dossier path.** `validatePromotionAction` / `refusePromotionAction` in `dictations/actions.ts` revalidate dictation routes but not `/students/[id]` — dossier D1 banner can show stale pending state after grid mutation until manual reload.

- **Source:** `deferred-work.md` lines 263–264; `dictations/actions.ts:130+`; `students/actions.ts:69-74` (dossier paths revalidated from students actions only).
- **Disposition:** fix now — add `revalidatePath(\`/students/${studentId}\`)` to grid promotion actions or share `revalidateDossierPromotionPaths`.

**`page.test.tsx` is the epic's test gravity well** (+676 net, 676 lines). Covers dossier composition across stories 4-1 through 4-4; individual component tests exist for curve, banner, presentation.

- **Source:** `git_evidence.py`; `wc -l page.test.tsx`.
- **Disposition:** accept — test volume tracks integrated dossier page; not production god-class.

### God-class / size growth

**`class-grid.tsx` unchanged in net churn** (+0 net, still 595 lines). Epic 3 retro item 17 (extract promotion row UI) was not executed; Epic 4 added only prop-alignment fixes (`cb87b088`).

- **Source:** `git_evidence.py` — `class-grid.tsx` net 0; `wc -l` 595 lines.
- **Disposition:** defer — extraction still warranted before Epic 5 mobile surfaces add more promotion chrome; not blocking Epic 4 acceptance.

**`students/[id]/page.tsx` at 173 lines** — reasonable orchestration page; grew +173 net across 9 commits as stories layered curve, banner, override, and RDV parents CTA.

- **Source:** `wc -l`; `git_evidence.py`.
- **Disposition:** accept.

**No production file crossed a blocking size threshold.** Highest service churn: `reevaluate-pending-promotion.ts` (162 lines), `override-student-level.ts` (122 lines) — both scoped and well-tested.

- **Source:** `wc -l` on top-ranked service files.
- **Disposition:** accept.

### Pattern divergence

**E2E coverage remains auth-only smoke.** Playwright (`e2e/smoke.spec.ts`, 29 lines) covers login/register/redirect — no dossier, Alertes, or presentation flow. Epic 2 item 9 and Epic 3 item 15 still open.

- **Source:** `e2e/smoke.spec.ts`; sprint-status `epic-2-retro-item-9`, `epic-3-retro-item-15`.
- **Disposition:** defer — propose Epic 5 pre-flight or dedicated QA story.

**`saveDictation` integration test still absent.** Inherited from Epic 3; Epic 4 story 4-5 added `dictation-save.test.ts` coverage for FR31 but not full transaction rollback.

- **Source:** `deferred-work.md` lines 146–158; sprint-status `epic-3-retro-item-16`.
- **Disposition:** defer.

**D3+ button still not in keyboard Tab order.** Story 3-6 deferral; Epic 4 did not address.

- **Source:** `deferred-work.md` lines 218–219; sprint-status `epic-3-retro-item-18`.
- **Disposition:** defer — accessibility pass.

### Spec-to-implementation reconciliation

| Requirement | Status | Evidence |
|-------------|--------|----------|
| Auto-generated per-student dossier (FR23) | met | `app/(dashboard)/students/[id]/page.tsx`, `get-student-dictation-history.ts` |
| Hero curve + collapsed dictation table (FR24–FR25) | met | `global-success-curve.tsx`, `dictation-history-table.tsx`, `dossier-layout.ts` |
| D1 promotion banner Valider/Refuser (FR26, FR29, FR31) | met | `promotion-banner.tsx`, `students/actions.ts` |
| Manual level override + history (FR27, FR33) | met | `level-dot-picker.tsx`, `override-student-level.ts`, `level-history-list.tsx` |
| Promotion detection rules (FR28) | met | `lib/domain/promotion.ts`, `reevaluate-pending-promotion.ts`, commit `0ec2419` |
| Alertes queue D2 (FR32) | met | `alerts/page.tsx`, `alerts-promotion-queue.tsx`, `nav-tabs.tsx` badge |
| Presentation mode C3 (FR34–FR36) | met | `presentation-mode.tsx`, `present/page.tsx`, `dossier-presentation.ts` |
| Single pending promotion per student (FR30) | met | `reevaluate-pending-promotion.ts`, shared `validateStudentPromotion` / `refuseStudentPromotion` |
| Server-authoritative scoring (NFR3) | met | dossier/presentation read persisted snapshots; domain helpers format only |
| CHAMPIONS wordmark in presentation (UX-DR6) | met | `presentation-mode.tsx` — deferred-work notes polish items only |

**Gap — stale D1 banner after grid-side promotion action.** Cross-surface revalidation asymmetry between `dictations/actions.ts` and `students/actions.ts`.

- **Source:** `deferred-work.md` lines 263–264.
- **Disposition:** fix now (see duplication map).

**Gap — no E2E for dossier → presentation → exit or Alertes badge decrement.** Unit tests cover plumbing; browser-level pilot gate still missing.

- **Source:** `deferred-work.md` lines 7–8, 258–259; `e2e/smoke.spec.ts`.
- **Disposition:** defer.

### Diff-scope review (inline lenses)

**Edge case — concurrent validate/refuse from grid and dossier.** `validateStudentPromotion` is idempotent (first wins); no integration test proves race under concurrent server actions.

- **Source:** `epic-4-context.md` line 29; service tests mock DB.
- **Disposition:** defer — low probability for single-teacher workflow.

**Verification gap — presentation focus restoration after exit.** AC asks sensible focus restore on dossier link; implementation relies on browser default.

- **Source:** `deferred-work.md` lines 235–237.
- **Disposition:** defer — a11y polish.

**Adversarial — `dashboard-chrome.tsx` `endsWith("/present")` false-positive risk.** Only `/students/[id]/present` exists today.

- **Source:** `deferred-work.md` lines 239–241; route table from `npm run build`.
- **Disposition:** defer — tighten when new routes added.

**Verification gap — duplicate DB queries on `/alerts`.** Layout fetches pending count; page fetches full queue.

- **Source:** `deferred-work.md` lines 5–6.
- **Disposition:** accept — class-size volume negligible for MVP.

---

## Behavior verification

| Check | Result | Source |
|-------|--------|--------|
| `npm test --run` (champions-app) | 105 files, 718 tests passed | shell 08-31-2026 |
| `npm run build` | succeeded | shell 08-31-2026 |
| `npm run test:e2e` (Playwright smoke) | not re-run this retro | `e2e/smoke.spec.ts` — public auth only |
| Manual browser: dossier → curve → expand row | not exercised | — |
| Manual browser: D1 validate/refuse → history update | not exercised | — |
| Manual browser: Alertes queue → validate → badge decrement | not exercised | — |
| Manual browser: RDV parents presentation → Esc exit | not exercised | — |
| DB connectivity against Neon | not exercised | — |

Runtime behavior verification was **not performed** beyond automated unit/integration tests and build. Epic 4 acceptance is judged on code, tests, spec reconciliation, and deferred-work tracking.

---

## Previous-retro follow-through

### Epic 3 retrospective action items

| Item id | Action | Landed? | Evidence | Proposed status |
|---------|--------|---------|----------|-----------------|
| `epic-3-retro-item-14-compile-epic-4-context-md-from-planning` | Compile `epic-4-context.md` | **yes** | `_bmad-output/implementation-artifacts/epic-4-context.md` exists; sprint-status already `done` | `done` (already marked) |
| `epic-3-retro-item-15-extend-e2e-smoke-dictation-create-grid-e` | E2E dictation create → grid → save | **no** | `e2e/smoke.spec.ts` unchanged (29 lines, auth only) | — |
| `epic-3-retro-item-16-add-integration-test-for-savedictation-t` | `saveDictation` integration test | **no** | `deferred-work.md` lines 146–158; no new DB integration test | — |
| `epic-3-retro-item-17-extract-promotion-row-ui-from-class-grid` | Extract promotion row from `class-grid.tsx` | **no** | `class-grid.tsx` still 595 lines; Epic 4 net churn 0 | — |
| `epic-3-retro-item-18-schedule-accessibility-pass-d3-button-in` | D3+ Tab order | **no** | `deferred-work.md` lines 218–219 unchanged | — |

### Epic 2 / Epic 1 items still open (inherited)

| Item id | Action | Landed? | Evidence | Proposed status |
|---------|--------|---------|----------|-----------------|
| `epic-2-retro-item-9-add-e2e-smoke-test-register-to-onboardin` | E2E onboarding smoke | **partial** | Playwright present; no roster/dictation/dossier flow | `in-progress` (already marked) |
| `epic-1-retro-item-5-support-callbackurl-on-login-redirect` | callbackUrl | **no** | no `callbackUrl` in login flow | — |
| `epic-1-retro-item-6-create-shared-test-utilities-for-next-li` | shared next/link mocks | **no** | per-file mocks persist across Epic 4 tests | — |

---

## Action items

| # | Action | Owner | Disposition |
|---|--------|-------|-------------|
| 1 | Add `revalidatePath(\`/students/${studentId}\`)` (and `/alerts` paths) to grid `validatePromotionAction` / `refusePromotionAction` in `dictations/actions.ts` — or extract shared `revalidateDossierPromotionPaths` | dev | fix now |
| 2 | Extend E2E smoke: Élèves → dossier → RDV parents → exit (pilot gate continuation of Epic 2/3 items) | dev | fix now (pilot gate) |
| 3 | Add integration test for `saveDictation` transaction rollback and promotion insert (Epic 3 item 16) | dev | defer |
| 4 | Extract promotion row UI from `class-grid.tsx` (Epic 3 item 17) before Epic 5 mobile adds surfaces | dev | defer |
| 5 | Shared `usePromotionAction` hook to dedupe validate/refuse client logic across grid, dossier, roster, Alertes | dev | defer |
| 6 | Schedule accessibility pass: D3+ **+** Tab order + presentation focus restoration (Epic 3 item 18 + 4-7 deferrals) | dev | defer |
| 7 | Auth hardening: callbackUrl + rate limiting (Epic 1 items 5, 7; Epic 2 item 12) | pm | defer (pre-pilot) |

Items 1 and 2 are proposed remediation — not auto-applied. Human decides execution timing.

### Process lessons

- **`epic-4-context.md` pre-flight worked** — Epic 3 retro item 14 landed before build; all seven stories traced cleanly to declared FRs without spec archaeology mid-session.
- **Cross-surface promotion is the epic's integration risk** — D1, D2, D3+, and roster actions share services but revalidation paths diverged; grid→dossier stale banner is the concrete symptom.
- **`reevaluate-pending-promotion.ts` extraction was the right 4-5 move** — kept `dictation-save.ts` from growing further while hardening FR31; 441-line test file proves the refactor was testable.
- **`page.test.tsx` absorbs integration coverage** — dossier page tests grew with each story instead of E2E; fast feedback but pilot gate still depends on shallow Playwright smoke.
- **`deferred-work.md` continues to work** — verification gaps from 4-3, 4-6, 4-7 are visible without blocking story completion; Epic 5 inherits a clear debt list.

---

## Acceptance verdict

**Verdict:** `accepted-with-open-items`  
**Criteria basis:** declared in `epic-4-context.md` and `epics.md` Epic 4 section (FR23–FR36, NFR1/NFR3/NFR8).

**Evidence:**

- All seven stories `done`; `pending_stories` empty (`detect-epic --epic 4`, 08-31-2026).
- Epic goal — auto-generated dossiers, cross-surface promotion management, manual override with history, and RDV parents presentation — is demonstrably present in code with 718 passing tests and successful build.
- Declared criteria met with named open items (E2E extension, cross-surface revalidation, integration tests, class-grid extraction, a11y) explicitly deferred in `deferred-work.md` — not silent acceptance.

Blocking criteria: none. Open items are verification, E2E, revalidation hygiene, and component extraction — not missing epic deliverables.

---

## Open questions

1. Should `epic-4` status in `sprint-status.yaml` transition to `done` now, or when Epic 5 story 5-1 starts?
2. Is the shallow E2E smoke sufficient for teacher pilot, or is dossier/presentation-flow E2E a hard gate before Epic 5?
3. Should grid promotion actions import `revalidateDossierPromotionPaths` from `students/actions.ts`, or should revalidation helpers move to a shared `lib/revalidation/` module?
