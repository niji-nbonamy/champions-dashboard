export const MOBILE_NO_LEVELED_STUDENTS_MESSAGE =
  "Aucun élève nivelé pour saisir.";

export function formatMobileHubCaptureAriaLabel(dictationLabel: string): string {
  return `Saisir les erreurs pour ${dictationLabel}`;
}

export function formatMobileHubSummaryAriaLabel(dictationLabel: string): string {
  return `Voir le résumé de ${dictationLabel}`;
}
